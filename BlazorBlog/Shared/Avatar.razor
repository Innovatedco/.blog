@if(Url != null)
{
    <img width="30" height="30" src="@Url" class="avatar@(string.IsNullOrEmpty(Classes) ? string.Empty : " " + Classes)"/>
}

@code {
    [Parameter]
    public string Url { get; set; } = default!;
    [Parameter]
    public string Classes { get; set; } = default!;
}
